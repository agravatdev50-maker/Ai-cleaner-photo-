package com.smartphotocleaner

import io.flutter.embedding.android.FlutterActivity

/**
 * Main Android entry point.
 * All logic is handled in Flutter via photo_manager, permission_handler, and ML Kit plugins.
 */
class MainActivity : FlutterActivity()
